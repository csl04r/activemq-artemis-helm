apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "artemis.fullname" . }}-certs
data:
  {{- $files := .Files.Glob "certs/*.*" }}
  {{- range $path, $file := $files }}
  {{ base $path }}: |
{{ $.Files.Get $path | indent 4 }}
  {{- end }}